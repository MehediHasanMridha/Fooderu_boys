/* eslint-disable react/prop-types */

const Divider = ({ className }) => {
  return (
    <div
      className={`border w-[364px] h-[1px] border-[#D2D2D2;] ${className}`}
    ></div>
  );
};

export default Divider;
